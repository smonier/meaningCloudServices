import { useEffect, useRef } from 'react';
export var useEnterExitCallbacks = function useEnterExitCallbacks(isDisplayed, onExiting, onExited, onEntering, onEntered) {
  var previousIsDisplayed = useRef(null);
  useEffect(function () {
    if (typeof previousIsDisplayed.current !== 'undefined') {
      if (!isDisplayed && previousIsDisplayed.current) {
        if (onExiting) {
          onExiting();
        }

        if (onExited) {
          onExited();
        }
      }

      if (isDisplayed && !previousIsDisplayed.current) {
        if (onEntering) {
          onEntering();
        }

        if (onEntered) {
          onEntered();
        }
      }
    }

    previousIsDisplayed.current = isDisplayed;
  }, [isDisplayed, onEntered, onEntering, onExited, onExiting, previousIsDisplayed]);
};
//# sourceMappingURL=useEnterExitCallbacks.js.map
