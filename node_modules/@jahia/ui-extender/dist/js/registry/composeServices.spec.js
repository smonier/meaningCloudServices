"use strict";

var _composeServices = require("./composeServices");
describe('composeServices', function () {
  it('should replace values', function () {
    var action = (0, _composeServices.composeServices)({
      value: 1
    }, {
      value: 2
    });
    expect(action.value).toEqual(2);
  });
  it('should concatenate arrays', function () {
    var action = (0, _composeServices.composeServices)({
      array: [1]
    }, {
      array: [2]
    });
    expect(action.array).toEqual([1, 2]);
  });
  it('should call the two action function', function () {
    var action = (0, _composeServices.composeServices)({
      onInit: function onInit(context) {
        context.tata = 45;
      }
    }, {
      onInit: function onInit(context, previous) {
        previous(context);
        context.toto = 42;
      }
    });
    var context = {};
    action.onInit(context);
    expect(context).toEqual({
      toto: 42,
      tata: 45
    });
  });
});
//# sourceMappingURL=composeServices.spec.js.map
